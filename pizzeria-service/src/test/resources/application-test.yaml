spring:
  main.banner-mode: off

  r2dbc:
    url: r2dbc:h2:mem:///pizzeria_service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    url: jdbc:h2:mem:pizzeria_service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    user: sa
    password:

jwt:
  secret: test-jwt-secret-minimum-32-characters-long
  expiration-ms: 3600000

logging.level:
  liquibase: WARN
  org.springframework: WARN
  io.r2dbc: WARN
